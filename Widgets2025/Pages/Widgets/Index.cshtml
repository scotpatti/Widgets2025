@page
@model Widgets2025.Pages.Widgets.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Widget[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Widget[0].CurrentPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Widget) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CurrentPrice)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.WidgetId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.WidgetId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.WidgetId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
